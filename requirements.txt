# AutoCast AI - Python 运行时依赖
# 用法：python -m pip install -r requirements.txt
#
# 注意：Python 3.10+ 推荐，3.11 测试通过。
# Windows 用户：先安装 https://www.python.org/downloads/windows/ (勾选 Add to PATH)。

# ── 核心库 ─────────────────────────────────────────────────────────
DrissionPage>=4.1.0            # 浏览器自动化 (登录/采集)
openai>=1.0.0                  # LLM 调用
httpx>=0.27.0                  # 异步 HTTP
urllib3>=2.0
websockets>=12.0               # IM bridge

# ── 语音识别与处理 (ASR/STT) ──────────────────────────────────────
# SenseVoice 多语种语音识别，用 sherpa-onnx 纯 ONNX 推理（无 torch）。
# 之前用 funasr+torch（torch 在 Windows ~1.5GB）会把内置 Python 撑到 >2GB，
# 导致 NSIS 安装包 32 位偏移溢出而打包失败。sherpa-onnx wheel 仅 ~77MB。
# 模型 model.int8.onnx(~228MB) 不打进安装包，运行时从 modelscope.cn 按需下载。
sherpa-onnx>=1.10.0

# ── 微信聊天监控 (WeChat Monitor) ─────────────────────────────────
# 微信 4.0+ 加密库的独立解密（SQLCipher-v4：PBKDF2-HMAC-SHA512 + AES-256-CBC）。
# 不依赖 WeFlow 的任何原生库。
pycryptodome>=3.20
pilk>=0.2.4                    # SILK_V3 语音解码（微信语音 → PCM/WAV，点击播放）
zstandard>=0.22               # 解压 WCDB zstd 压缩的 message_content（部分文本/卡片消息）

# ── 知识库 (KB) ────────────────────────────────────────────────────
numpy==1.26.4                  # Required for X86_V1 compatibility
pyarrow==16.1.0                # Compatible with numpy 1.x
lancedb==0.11.0                # Stable version for numpy 1.x
pandas>=2.0.0                  # Data processing
pypdf>=4.0.0                   # PDF Parsing
python-docx>=1.1.0             # Word(.docx) 解析
openpyxl>=3.1.0               # Excel(.xlsx) 解析（pandas read_excel 引擎）
xlrd>=2.0.1                    # Excel 旧格式(.xls) 解析

# ── 抖音子模块 ─────────────────────────────────────────────────────
# scripts/DouyinComment/requirements.txt
pyyaml>=6.0
tqdm
pillow
pillow-heif
huggingface_hub

# scripts/DouyinBarrage/requirements.txt
requests>=2.33.1,<3.0
websocket-client>=1.9.0,<2.0
protobuf>=7.34.1,<8.0
proto-plus>=1.27.0,<2.0

# ── 视频生成 (Video Generation) ───────────────────────────────────
fal-client>=0.4.0              # fal.ai API 客户端（旧生成式链路，暂留）

# ── MoneyPrinterTurbo 视频引擎（素材拼接成片）────────────────────
# 主题→脚本→关键词→TTS→字幕→下载素材→MoviePy/FFmpeg 拼接成片。
moviepy==2.2.1                 # 视频拼接、字幕烧录、BGM 合成
edge_tts==7.2.7                # 免费 TTS（默认配音 + 字幕时间轴对齐）
pydub==0.25.1                  # 音频时长/转码（moviepy/edge 辅助）
loguru>=0.7                    # MPT 引擎日志（输出到 stderr）
imageio-ffmpeg                 # moviepy 的 ffmpeg 后端解析
# faster-whisper 可选：仅当字幕模式选 whisper 时需要，首次运行会下载模型（~250MB+）。
# 默认字幕走 edge 模式，无需安装；如需更精准字幕再取消下一行注释并重跑 prepare:python。
# faster-whisper==1.1.0
